{
  "name": "nurah-islamic-finance",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-sites-build.mjs clean && vite build --outDir dist/client --emptyOutDir && node scripts/prepare-sites-build.mjs finalize",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@gsap/react": "^2.1.2",
    "gsap": "^3.15.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "prettier": "^3.8.1",
    "vite": "^8.0.16"
  }
}
